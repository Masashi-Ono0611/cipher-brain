---
---

Comment-only correction in `src/lib/wizard.ts`: the recovery kit's design was
attributed to the wrong project. Nothing the CLI does changes, so this ships no
user-visible change — an empty changeset per `CONTRIBUTING.md`.
