---
'cipher-brain': patch
---

The mascot got a sharper hood. Same face, same semantics — the lens pair still
squares for verified and slips round for unproven (#147/#194), the mouth still
carries the mood — but the hood peak is now a true mirror (`_,-^-,_`), the brim
insets cleanly under the face edges, and the chin closes with quotes instead of
a flat underscore run. The #197 center math is untouched: the peak's `^` still
lands at column 8 over the face's 7.5 visual center, re-measured per column.
